# AI 旅行助手部署运行说明

## 安全提示

本交付包按需求保留了 `.env`，其中包含 DeepSeek、高德 MCP、微信小程序 AppSecret 和管理员初始密码等敏感值。

- 只发送给可信的部署人员，不要上传公开 Git 仓库、网盘公开链接或群聊。
- 每次对外交付后，建议在对应平台轮换密钥。
- 生产部署前必须修改 `ADMIN_INITIAL_PASSWORD`，并限制 `.env` 的文件权限。

## 系统要求

- macOS
- Python 3.12+
- Node.js 18+
- PostgreSQL 15+
- pgvector
- 微信开发者工具（需运行小程序时）

Homebrew 环境可使用：

```bash
brew install python@3.12 node postgresql@17 pgvector ollama
brew services start postgresql@17
brew services start ollama
ollama pull qwen3-embedding:0.6b
```

## 首次部署

### 最快方式

安装好系统要求后，直接双击 `一键快速部署.command`。它会自动完成依赖安装、数据库迁移、前端/小程序构建、服务启动和健康自检。

### 分步方式

1. 解压后进入“AI旅行助手”目录。
2. 双击 `首次安装.command`。macOS 如果阻止运行，请右键脚本选择“打开”。
3. 脚本会安装 Python、管理端、小程序和 12306 MCP 依赖，然后创建 `deeptravel` 数据库并执行 Alembic 迁移。
4. 若接收方的 PostgreSQL 用户、端口或密码不同，先修改 `.env` 中的 `DATABASE_URL`。
5. 双击 `启动AI旅行助手.command`。

服务启动后可双击 `部署自检.command`，一次检查后端、Search MCP、管理端和小程序构建产物。

启动后地址：

- 管理端：<http://127.0.0.1:5173/>
- FastAPI 状态：<http://127.0.0.1:8000/health>
- Search MCP：<http://127.0.0.1:8100/health>
- 12306 MCP：`http://127.0.0.1:8200/mcp`

停止服务时双击 `停止AI旅行助手.command`。运行日志位于 `logs/`。

## 交给 AI 代理部署

普通聊天机器人不能直接操作电脑。接收者需使用具有终端权限的 Codex、Claude Code、Cursor Agent 或其他本地 AI 编程代理，并将工作目录指向本目录。

将 `AI部署提示词.md` 的内容发给 AI 即可。AI 会先向接收者申请必要的联网和软件安装权限，补齐 PostgreSQL、pgvector、Ollama 等系统依赖，再执行 `一键快速部署.command --no-pause`、读取 `logs/` 定位失败原因，并以 `部署自检.command --no-pause` 作为最终验证。

如需真机调试，还要让 AI 执行 `构建微信小程序.command --no-pause`，并在微信开发者工具中导入重新生成的 `miniapp/dist/build/mp-weixin` 目录。

### 不同网络的临时真机调试（Cloudflare Tunnel）

手机和后端电脑不在同一个网络时，可以双击 `启动Cloudflare体验.command`。脚本会：

1. 检查并在 macOS Homebrew 中安装 `cloudflared`；
2. 确认本地 FastAPI 后端已启动；
3. 创建免费的临时 `https://*.trycloudflare.com` 公网地址；
4. 用该地址重新构建微信小程序，并输出导入目录。

然后在微信开发者工具导入 `miniapp/dist/build/mp-weixin`，点击“真机调试”生成二维码。体验者不需要与后端电脑连接同一网络，但本机后端、Ollama、Search MCP 和 Cloudflare 隧道必须持续运行。

临时地址每次重建或隧道重启都可能变化，地址变化后必须重新运行脚本并重新导入构建产物。开发者工具调试时勾选“不校验合法域名、TLS 版本及 HTTPS 证书”；随机 `trycloudflare.com` 地址不适合直接上传为长期体验版/正式版。长期使用请准备固定 HTTPS 域名并在微信公众平台配置 `request 合法域名`。

停止时双击 `停止AI旅行助手.command`，它会同时关闭 Cloudflare 隧道。

## 微信小程序

1. 确保手机与后端电脑在同一局域网。
2. 每次更换网络后，双击 `构建微信小程序.command`，脚本会把当前电脑局域网 IP 写入构建产物。
3. 在微信开发者工具导入 `miniapp/dist/build/mp-weixin`。
4. 微信公众平台的小程序 AppID 必须与 `miniapp/src/manifest.json` 及 `project.config.json` 一致。
5. 开发/真机调试可在开发者工具中勾选“不校验合法域名”。体验版或正式版不能依赖局域网 HTTP，需将 FastAPI 部署到公网 HTTPS 域名，然后用以下方式构建：

```bash
cd miniapp
VITE_API_BASE_URL=https://api.example.com npm run build:mp-weixin
```

## 生产部署注意事项

- FastAPI 应使用 systemd、supervisor 或容器平台托管，不建议直接使用开发启动脚本承载公网流量。
- 管理端执行 `npm run build`后，将 `frontend/dist/` 由 Nginx 等静态服务托管，并把 `/api` 反向代理到 FastAPI。
- FastAPI、Search MCP、12306 MCP、PostgreSQL 和 Ollama 应使用内网通信，只公开必要的 HTTPS 入口。
- 若使用 Ollama Embedding，需额外执行 `ollama pull qwen3-embedding:0.6b`。
- 数据库内容不包含在源码包中，迁移脚本会创建表结构，原有知识库和用户数据需单独导出/导入。
